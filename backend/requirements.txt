fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
python-multipart==0.0.20

# ASR (自动语音识别) 和 Diarization (声纹分离) 依赖
faster-whisper==1.0.2
pyannote.audio==3.1.1

# 音频处理
pydub==0.25.1

# 若使用 GPU，需安装：
# torch>=2.0 with CUDA support
# 或使用 CPU 版本：
# torch>=2.0
